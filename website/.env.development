# Loaded automatically by Vite in development mode (`vite` / `vite build --mode
# development`). Points the Photon Grove attribution link at the dev homepage.
# Production builds omit this and fall back to https://photon-grove.com.
VITE_PHOTON_GROVE_URL=https://dev.photon-grove.com
